-- Intentionally no-op.
--
-- A blanket repair of zero-credit waiting_for_round students is unsafe because
-- older close-round logic represented a valid reserved continuation as:
--
--   status = waiting_for_round
--   remaining_credits = 0
--   latest class_enrollments.next_level_consumed_on_close = true
--
-- Those rows must be audited case-by-case against payment/manual records before
-- changing status or payment_cycles.
SELECT 1;
